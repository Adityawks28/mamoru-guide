/**
 * router.ts — Hash-based view router (stub for Phase 0)
 *
 * Phase 1 will implement the full router logic.
 * For now this module just exports the expected function signature
 * so other modules can import it without compile errors.
 */

/** Initialise the router. Called once on DOMContentLoaded in main.ts. */
export function initRouter(): void {
  // Phase 1 implementation goes here
}
