/**
 * placeholder.test.ts
 *
 * A minimal smoke test that confirms the test runner is wired up correctly.
 * This file will be replaced by real tests as each phase lands.
 *
 * HOW TESTS WORK (for learning):
 *   describe() — groups related tests together, like a folder label
 *   it()       — one individual test case ("it should do X")
 *   expect()   — makes an assertion; if it's wrong the test fails
 *   toBe()     — checks strict equality (===)
 */

describe('test runner', () => {
  it('is configured correctly', () => {
    expect(true).toBe(true);
  });
});
